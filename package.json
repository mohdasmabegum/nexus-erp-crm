{
  "name": "nexus-erp-crm-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build --workspace=server",
    "build:vercel": "npm install && cd server && npm install && npx prisma generate && cd ../client && npm install && npm run build",
    "start": "npm run start --workspace=server",
    "dev": "npm run dev --workspace=server"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0",
    "@vercel/node": "^3.0.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/jsonwebtoken": "^9.0.7",
    "@types/morgan": "^1.9.9",
    "@types/node": "^22.10.0",
    "prisma": "^6.0.0",
    "typescript": "^5.7.2"
  }
}
